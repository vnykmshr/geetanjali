# Grafana Alerting Notification Policies
# Routes alerts to contact points

apiVersion: 1

policies:
  - orgId: 1
    receiver: email-alerts
    group_by:
      - grafana_folder
      - alertname
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
