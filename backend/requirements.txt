# Geetanjali Backend Dependencies

# Web Framework
fastapi==0.123.5
uvicorn[standard]==0.38.0
python-multipart==0.0.20

# Database
sqlalchemy==2.0.44
alembic==1.17.2
psycopg2-binary==2.9.11  # PostgreSQL driver

# Pydantic (included with FastAPI but pinned for clarity)
pydantic==2.12.5
pydantic-settings==2.12.0
email-validator==2.3.0  # Required for pydantic EmailStr validation

# Vector Database
chromadb==1.3.5

# Embeddings
sentence-transformers==5.1.2

# LLM Clients
httpx>=0.27.0  # For Ollama API calls (chromadb 1.3.5 requires >=0.27.0)
anthropic==0.75.0  # Anthropic Claude API

# Testing
pytest==9.0.1
pytest-asyncio>=1.0.0  # pytest 9.x requires pytest-asyncio 1.x
pytest-cov==4.1.0

# Code Quality
black==25.11.0
flake8==7.3.0
mypy==1.19.0
types-requests==2.32.4.20250913  # Type stubs for requests
types-PyYAML==6.0.12.12  # Type stubs for PyYAML
types-redis==4.6.0.20241004  # Type stubs for redis

# Authentication
python-jose[cryptography]==3.5.0  # JWT tokens
passlib[bcrypt]==1.7.4  # Password hashing
bcrypt==4.3.0  # pinned due to passlib 1.7.4 incompatibility with bcrypt 5.0.0

# Utilities
python-dotenv==1.2.1
tenacity==9.1.2  # Retry logic for robustness
slowapi==0.1.9  # Rate limiting

# Redis Cache & Task Queue
redis==5.0.1
rq==1.16.0  # Redis Queue for background tasks

# Data Ingestion
beautifulsoup4==4.14.3  # HTML parsing
lxml==6.0.2  # XML/HTML parser
pyyaml==6.0.3  # YAML config loading
indic-transliteration==2.3.75  # Sanskrit IAST transliteration
requests==2.32.5  # HTTP client for fetching
urllib3>=2.5.0  # CVE-2025-50181, CVE-2025-50182 fix

# Email
resend>=2.5.0  # Transactional email service

