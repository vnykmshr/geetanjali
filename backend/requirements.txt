# Geetanjali Backend Dependencies

# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9  # PostgreSQL driver

# Pydantic (included with FastAPI but pinned for clarity)
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0  # Required for pydantic EmailStr validation

# Vector Database
chromadb==1.3.5

# Embeddings
sentence-transformers==5.1.2

# LLM Clients
httpx>=0.27.0  # For Ollama API calls (chromadb 1.3.5 requires >=0.27.0)
anthropic==0.40.0  # Anthropic Claude API

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0

# Code Quality
black==25.11.0
flake8==7.0.0
mypy==1.8.0
types-requests==2.31.0.10  # Type stubs for requests
types-PyYAML==6.0.12.12  # Type stubs for PyYAML
types-redis==4.6.0.11  # Type stubs for redis

# Authentication
python-jose[cryptography]==3.5.0  # JWT tokens
passlib[bcrypt]==1.7.4  # Password hashing
bcrypt==5.0.0  # Pin compatible version (5.x has issues with passlib 1.7.4)

# Utilities
python-dotenv==1.0.0
tenacity==8.2.3  # Retry logic for robustness
slowapi==0.1.9  # Rate limiting

# Redis Cache & Task Queue
redis==5.0.1
rq==1.16.0  # Redis Queue for background tasks

# Data Ingestion
beautifulsoup4==4.12.3  # HTML parsing
lxml==5.1.0  # XML/HTML parser
pyyaml==6.0.1  # YAML config loading
indic-transliteration==2.3.44  # Sanskrit IAST transliteration
requests==2.31.0  # HTTP client for fetching

