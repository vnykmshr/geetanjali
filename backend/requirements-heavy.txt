# Heavy dependencies - installed first for better caching
# These rarely change and take longest to install
# Note: torch CPU-only is installed separately in Dockerfile to avoid CUDA downloads

# Embeddings (requires torch, installed in Dockerfile)
sentence-transformers==5.1.2

# Vector Database
chromadb==0.5.0

# NumPy compatibility (pinned <2.0 for chromadb 0.5.0)
numpy>=1.24.0,<2.0
